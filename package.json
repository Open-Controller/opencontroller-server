{
  "name": "@open-controller/server",
  "version": "1.1.2",
  "description": "A simple server for Open Controller houses",
  "main": "dist/index.js",
  "author": "PJTSearch",
  "license": "GPL-3.0-only",
  "scripts": {
    "build": "tsc ./src/index.ts --outDir ./dist --lib esnext,dom -d --esModuleInterop --declaration"
  },
  "dependencies": {
    "@types/koa": "^2.11.3",
    "koa": "^2.13.0"
  },
  "devDependencies": {
    "@open-controller/lib": "^0.24.0",
    "typescript": "^3.8.3"
  },
  "peerDependencies": {
    "@open-controller/lib": "^0.24.0"
  }
}
